<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.friday.mybatis.mapper.StudentMapper">

    <select id="selectById" resultType="com.friday.mybatis.pojo.data.StudentDO">
        SELECT * FROM student WHERE id = #{id}
    </select>

</mapper>